datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Envio {
  id         Int    @id @default(autoincrement())
  pedidoId   Int
  usuarioId  Int
  estado     String
}
