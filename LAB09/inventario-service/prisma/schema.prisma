datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Inventario {
  id     Int    @id @default(autoincrement())
  nombre String
  stock  Int
}
